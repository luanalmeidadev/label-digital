import AdminPagePlaceholder from "@/components/admin/AdminPagePlaceholder";

export default function EntregasPage() {
  return (
    <AdminPagePlaceholder
      eyebrow="Logística"
      title="Entregas"
      description="Configure bairros atendidos e taxas de entrega."
    />
  );
}